import * as startup from '../steps/startup.steps.js';
// import * as _ from '../steps/common.steps.js';

before(() => {
    startup.shouldLoad();
});

describe('player', () => {
    it('movement', () => {});
});
